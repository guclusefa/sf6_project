{% extends 'base.html.twig' %}

{% block title %}Personne{% endblock %}

{% block body %}
    <h1>Personne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personne.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ personne.firstname }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ personne.name }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ personne.age }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ personne.image }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ personne.createdAt ? personne.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ personne.updatedAt ? personne.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ personne.deletedAt ? personne.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_test_index') }}">back to list</a>

    <a href="{{ path('app_test_edit', {'id': personne.id}) }}">edit</a>

    {{ include('test/_delete_form.html.twig') }}
{% endblock %}
